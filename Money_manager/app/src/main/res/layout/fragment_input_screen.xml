<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".InputScreenFragment">

    <LinearLayout
        android:id="@+id/ll_option_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent">

        <Button
            android:id="@+id/button_enter_expense"
            android:layout_margin="10dp"
            android:layout_width="160dp"
            android:layout_height="wrap_content"
            android:text="Enter Expense"
            android:backgroundTint="@color/gray"
            app:layout_constraintTop_toTopOf="parent"
            />
        <Button
            android:id="@+id/button_enter_income"
            android:layout_margin="10dp"
            android:layout_width="160dp"
            android:layout_height="wrap_content"
            android:text="Enter Income"
            android:backgroundTint="@color/gray"
            app:layout_constraintTop_toBottomOf="@+id/button_enter_expense"
            />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/fl_input_page_fragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/ll_option_buttons"
        >
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>